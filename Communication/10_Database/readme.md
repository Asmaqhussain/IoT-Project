In this project, I implemented a system where an ESP32 sends sensor data using MQTT. A Python program running on the computer receives the data and stores it in a SQLite database.

The system simulates multiple remote sensors sending measurements to a central computer.

For data processing, I created a graph using matplotlib to visualize how the sensor values change over time.

This demonstrates a complete IoT data pipeline including communication, storage, and analysis.